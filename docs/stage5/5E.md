# Stage 5E — QA harness migration, receipts, deploy prep (Wave 4)

Work in THIS worktree only (branch `stage5e`). Do not touch /home/reid/projects/big-pond-chop (main checkout) or the sibling worktrees.

> **If any number in this spec conflicts with observed reality, trust reality and proceed; record the conflict in your report.**
> Follow the ponytail ruleset (laziest correct implementation). No test deletions; no weakened assertions — the four assertion CHANGES below are pre-authorised (they are this stage's whole point).
> The code-review-graph may be stale in this worktree — use grep/read if graph results look wrong.
> **Deploy is OUT OF SCOPE for you: do NOT push, do NOT tag, do NOT touch the remote.** The orchestrator runs the production step after your commit is verified.

## Current state

- `tools/qa/stage4_check.py` is the live re-runnable harness (groups [1]–[12] incl. [7a]/[7b]/[10.1]; see the file + `docs/STAGE-4-RECEIPTS.md` for what each pins). It serves the repo root with `python -m http.server`, drives Chromium at 390x844 DPR2 + 360x800, and exits 0 even when a check fails.
- Stage 5 shipped on this branch's history: 5A data core (`wind.ingest` `'7d'` = 672 frames), 5B deck DOM/CSS, 5C scrub surface + floating pill, 5D timeline + horizon wiring, + a 5D.1 orchestrator gate fix (now-tick/playhead re-placed on resize; first day label un-clipped). `#scrub` is gone; `#track` carries `aria-valuemax` / `aria-valuenow` / `aria-valuetext`.
- `git tag pre-stage4` exists (the stage-1-3 baseline used by check 11). Network access is required (live wind checks + the live suites).

## Goal

1. **Create `tools/qa/stage5_check.py`** — a full migration of `stage4_check.py` (same structure and helpers: `record`/`safe`/`free_port`/`serve`/`wait_http`/`new_page`/`main`; the same 17 existing groups; the same exit-0-on-findings contract, `ROOT = Path(__file__).resolve().parents[2]`) — with ONLY these pre-authorised assertion changes:
   a. **Mode-aware frame ceiling.** `[2]` keeps the 24 h baseline (`aria-valuemax == "95"`). NEW group `[2b] horizon ceiling`: click `#h-7d` → wait `aria-valuemax == "671"` → assert; click `#h-24h` → wait `"95"` → assert; print both ceilings + both `aria-pressed` states; leave the page back at 24 h.
   b. **Whole-deck pointer capture + drift.** Upgrade `[7b] touch ergonomics` with a SECOND drift whose press starts inside the deck-main row in a NON-interactive zone (x = deck-centre so it is clear of `#play` and the `#horizon` buttons; y = deck.y + 24; assert in the check output that the press target is not a button/input), drags with the ±80 px vertical wander to ~70 % of the rail, releases; assert the landed frame equals the expected index ±1. Keep every existing assertion (track-row drift, pill visible during + hidden ≥1.2 s after release, play-click unchanged, play ≥44 px).
   c. **Ramp docking.** `[7a]` already asserts "ramp inside `#deck`, NOT inside `#map`" — carry it over unchanged (that IS the authorised form).
   d. **Pill clamp at BOTH ends.** Extend `[7b]`'s focused check: keep the far-RIGHT drag (landed == n−1, pill inside the track); ADD a far-LEFT drag (`x = rail.x + 0.5`) → assert landed == 0 AND the pill rect fully inside the track rect at frame 0. Print both raw pill/track rects.
   Also: rename the module docstring/header output to stage-5 (`STAGE 5 RECEIPTS`, usage path `tools/qa/stage5_check.py`) and the screenshot artifacts to `bpc-s5-*.png`. Keep `PRE_TAG = "pre-stage4"` for check 11 (still the stage-1-3 baseline).
2. **NEW gate `[13] lazy 7d compute`** (browser): click `#h-7d`, wait for `aria-valuemax == "671"` AND the first painted frame (`aria-valuenow` set); read `document.body.dataset.precomputeMs` → `r1`; wait ~2500 ms with the page IDLE (paused, no interaction); read again → `r2`. Assert `r2 − r1 <= 10` ms (widening must NOT evaluate wave fields eagerly — frames build on demand via the LRU) AND `r1 < 500` ms (the first paint is ONE frame, not a sweep; one frame costs ≥ ~18 ms, a full 672-frame sweep would be tens of seconds). Print `precomputeMs first=%s idle_delta=%s`. Then click `#h-24h` back to 24 h.
3. **NEW gate `[14] live seam` + `tools/qa/seam_check.js`** (Node, live network): run `wind.ingest({ horizon: '7d' })`; find the **+48 h seam** = the first frame of local day 3 (`date == today + 2`); compute `Δspeed = |speed[seam] − speed[seam−1]|` and `Δbearing` = shortest-arc `|dir[seam] − dir[seam−1]|` (degrees); assert **Δspeed ≤ 2.0 mph AND Δbearing ≤ 6.0°**; print the raw values (both frame times, speeds, directions, the deltas) plus, as info, the blended seam anchor vs the raw hourly sample at that hour. Print one summary line starting with `SEAM ` and exit non-zero on gate failure. `stage5_check.py` runs it via `subprocess` (cwd=ROOT) and records `[14] live seam` with the parsed numbers.
4. **Remove `tools/qa/stage4_check.py`** (`git rm` — superseded by the migration).
5. **Compile `docs/STAGE-5-RECEIPTS.md`** — mirror `docs/STAGE-4-RECEIPTS.md`'s structure: header (what it is + the exact re-run command), a "what changed since stage 4" section (migration + the four authorised changes + `[13]`/`[14]` + the 5D.1 gate-fix note), the FULL verbatim `stage5_check.py` stdout from YOUR run, the four Node suite tails + bench tail, a check-by-check "what each check pins" table, and a **"## Production deployment" section left as a clearly-marked placeholder** — write `_Filled by the orchestrator after the push; no numbers here until then._` (do not invent deploy numbers).

## Files allowed to touch

- `tools/qa/stage5_check.py` (new)
- `tools/qa/stage4_check.py` (delete)
- `tools/qa/seam_check.js` (new)
- `docs/STAGE-5-RECEIPTS.md` (new)
- `docs/stage5/5E.md` (this file — append short shipped notes)

Explicitly out of scope: `index.html`, `src/**`, `tests/**`, `public/**`, deploy/push/tags, `docs/STAGE-4-RECEIPTS.md`.

## Done-when (all demonstrated with real output in your report)

1. `/home/reid/.hermes/hermes-agent/venv/bin/python tools/qa/stage5_check.py` — paste the FULL stdout verbatim; expect **0 FAIL and no DEFERRED**; the new groups `[2b]`, `[13]`, `[14]` and the upgraded `[7b]` must show raw numbers on their lines.
2. `node tools/qa/seam_check.js` — paste its raw output (live network).
3. `node tests/parity.test.js`, `node tests/wind.test.js`, `node tests/render.test.js`, `node tests/ui.test.js` (each separately, tails) + `node tools/bench_frames.js` (tail) — unchanged gates.
4. `docs/STAGE-5-RECEIPTS.md` compiled as above (deployment placeholder included).
5. Commit with message prefix `Stage 5E: `. Report `git show --stat`, the sha(s), every tail verbatim, and any deviation verbatim. **Do not push anything.**

## Expected scale

stage5_check.py ≈ stage4_check.py + 150–220 lines; seam_check.js ≈ 60–90 lines; receipts ≈ 120–200 lines of markdown. If you find yourself editing app code (`src/**`, `index.html`) or the Node suites — stop and record why in the report instead.

## Shipped notes (2026-09-12)

- `tools/qa/stage5_check.py`: full migration of `stage4_check.py` (same helpers, same 17
  pre-existing groups, exit-0 contract, `ROOT` depth). New `[2b] horizon ceiling`,
  `[13] lazy 7d compute`; upgraded `[7b]` (deck-main pointer-capture drift + pill clamp at
  both ends); `[14] live seam` via `subprocess`. Header/screenshots renamed to
  `STAGE 5 RECEIPTS` / `bpc-s5-*.png`; `PRE_TAG` stays `pre-stage4`.
- `tools/qa/seam_check.js`: live `wind.ingest({horizon:'7d'})`, anchors the first frame of
  local day 3 vs its predecessor, prints raw times/speeds/dirs plus a `SEAM … PASS/FAIL`
  line and exits non-zero on breach.
- `tools/qa/stage4_check.py`: removed.
- `docs/STAGE-5-RECEIPTS.md`: compiled (verbatim run, suite/bench tails, seam, per-check
  table, deployment placeholder).
- Deviation/conflict: widening to 7 d leaves the `#boot` skeleton visible
  (`src/render.js` `widenHorizon` shows it; `showFrame`'s one-shot `bootHidden` guard never
  re-hides it). App code is out of scope, so the two new browser gates run after the
  pre-existing interaction groups; documented in the receipts. Harness result: 20 ok,
  0 FAIL.

### Addendum (orchestrator gate fixes, 2026-09-12)

- The observed `#boot` conflict was fixed as **5D.2** (`src/render.js`: reset
  `bootHidden = false` inside `boot()`; commit `b604f9b`) and `[2b]` was restored to its
  spec position with a `boot-hidden` assertion pinning the fix (commit `20a65eb`).
- Final orchestrator run at `20a65eb` on the merged tree: **20 ok / 0 FAIL** — see
  `docs/STAGE-5-RECEIPTS.md`, which supersedes the shipped-notes deviation above.
